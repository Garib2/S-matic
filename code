from pymatgen.io.qe import PwInput

# Quantum ESPRESSO 입력 파일을 읽어서 구조 객체 생성
structure = PwInput.from_file("input_tmp.in").structure

# 구조를 CIF 파일로 저장
structure.to("structure.cif")

# CIF 파일을 VESTA 또는 다른 도구로 시각화