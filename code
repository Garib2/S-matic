from pymatgen.core import Structure
from pymatgen.io.cif import CifWriter

# Quantum ESPRESSO 입력 파일로부터 구조 로드
structure = Structure.from_file("input_file.in")

# 3D 시각화
structure.to(filename="structure.cif")